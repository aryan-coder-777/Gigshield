{
  "buildCommand": "cd frontend && npm install && expo export -p web",
  "outputDirectory": "frontend/dist",
  "devCommand": "cd frontend && expo",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}

